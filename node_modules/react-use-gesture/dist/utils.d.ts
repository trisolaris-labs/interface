import React from 'react';
import { Fn, EventOptions, Coordinates, FullGestureState, DistanceAngle, UseGestureEvent } from './types';
export declare const noop: () => void;
export declare const chainFns: (...fns: Fn[]) => Fn;
export declare const addV: <T extends number[]>(v1: T, v2: T) => T;
export declare const subV: <T extends number[]>(v1: T, v2: T) => T;
export declare const addListeners: (el: EventTarget, listeners: [string, Fn][], options: EventOptions) => void;
export declare const removeListeners: (el: EventTarget, listeners: [string, Fn][], options: EventOptions) => void;
interface ModifierKeys {
    shiftKey: boolean;
    altKey: boolean;
    metaKey: boolean;
    ctrlKey: boolean;
}
/**
 * Gets modifier keys from event
 * @param event
 * @returns modifier keys
 */
export declare function getModifierKeys(event: UseGestureEvent): ModifierKeys;
declare type ScrollEventData = Pick<FullGestureState<Coordinates>, 'xy'> & ModifierKeys;
/**
 * Gets scroll event data
 * @param event
 * @returns scroll event data
 */
export declare function getScrollEventData(event: UseGestureEvent): ScrollEventData;
declare type WheelEventData = Pick<FullGestureState<Coordinates>, 'xy'> & ModifierKeys;
/**
 * Gets wheel event data
 * @param event
 * @returns wheel event data
 */
export declare function getWheelEventData(event: UseGestureEvent<React.WheelEvent>): WheelEventData;
declare type PointerEventData = Pick<FullGestureState<Coordinates>, 'xy' | 'touches' | 'down' | 'buttons'> & ModifierKeys;
/**
 * Gets pointer event data
 * @param event
 * @returns pointer event data
 */
export declare function getPointerEventData(event: React.MouseEvent | React.TouchEvent | React.PointerEvent): PointerEventData;
declare type TwoTouchesEventData = Pick<FullGestureState<DistanceAngle>, 'da' | 'touches' | 'down' | 'origin'> & ModifierKeys;
/**
 * Gets two touches event data
 * @param event
 * @returns two touches event data
 */
export declare function getTwoTouchesEventData(event: React.TouchEvent): TwoTouchesEventData;
/**
 * Calculates velocity
 * @param delta the difference between current and previous vectors
 * @param delta_t the time offset
 * @param len the length of the delta vector
 * @returns velocity
 */
export declare function calculateVelocity(delta: number[], delta_t: number, len: number): number;
/**
 * Calculates velocities vector
 * @template T the expected vector type
 * @param delta the difference between current and previous vectors
 * @param delta_t the time offset
 * @returns velocities vector
 */
export declare function calculateVelocities<T extends number[]>(delta: T, delta_t: number): T;
/**
 * Calculates distance
 * @param movement the difference between current and initial vectors
 * @returns distance
 */
export declare function calculateDistance(movement: number[]): number;
/**
 * Calculates direction
 * @template T the expected vector type
 * @param delta
 * @param len
 * @returns direction
 */
export declare function calculateDirection<T extends number[]>(delta: T, len?: number): T;
interface Kinematics<T extends number[]> {
    velocities: T;
    velocity: number;
    distance: number;
    direction: T;
}
/**
 * Calculates all kinematics
 * @template T the expected vector type
 * @param movement the difference between current and initial vectors
 * @param delta the difference between current and previous vectors
 * @param delta_t the time difference between current and previous timestamps
 * @returns all kinematics
 */
export declare function calculateAllKinematics<T extends number[]>(movement: T, delta: T, delta_t: number): Kinematics<T>;
/**
 * Whether the browser supports GestureEvent (ie Safari)
 * @returns true if the browser supports gesture event
 */
export declare function gestureEventSupported(): boolean;
export {};
