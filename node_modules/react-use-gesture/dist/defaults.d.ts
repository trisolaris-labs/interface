import { GestureConfig, HandlerKey, CommonGestureState, StateObject, StateKey, GestureKey } from './types';
declare type MappedKeys = {
    [K in GestureKey]: {
        stateKey: StateKey;
        handlerKey: HandlerKey;
    };
};
/**
 * Some gestures might use the state key from another gesture (i.e. hover)
 * so mappedKeys is a commodity object to get the state key and handler key
 * for every gesture
 */
export declare const mappedKeys: MappedKeys;
export declare const defaultConfig: GestureConfig;
export declare const initialCommon: CommonGestureState;
export declare const initialState: StateObject;
export declare const genericEndState: Partial<CommonGestureState>;
export {};
